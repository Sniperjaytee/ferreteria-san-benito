{% load static tailwind_tags %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{% block title %}Ferretería San Benito{% endblock %}</title>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
    
    <!-- Tailwind CSS -->
    {% tailwind_css %}
</head>
<body class="bg-gray-50 relative min-h-screen flex flex-col">

    <!-- ======================= HEADER ======================= -->

    <header class="bg-secondary shadow-sm sticky top-0 z-50">
     <div class="container mx-auto px-4">
        <div class="flex flex-col md:flex-row items-center py-3 gap-4">
          <div class="hidden md:block flex-shrink-0">
            <a href="{% url 'core:index' %}">
              <h1 class="text-2xl font-bold text-primary">SAN BENITO</h1>
            </a>
          </div>
          <div class="flex-grow w-full md:w-auto">
            <div class="relative">
              <input type="text" placeholder="¿Qué buscas el día de hoy?" class="w-full py-2 px-4 pr-10 rounded-full border border-gray-300 focus:outline-none focus:ring-2 focus:ring-secondary"/>
              <button class="absolute right-3 top-2.5 text-gray-500">
                <i class="fas fa-search"></i>
              </button>
            </div>
          </div>
          <div class="hidden md:flex space-x-4">
            <div class="flex space-x-2">
              {% if user.is_authenticated %}<a href="{% url 'logout' %}" class="leading-normal px-4 py-1.5 text-sm rounded-full border border-primary text-primary hover:bg-primary hover:text-white transition">Cerrar sesi�n</a>{% else %}<a href="{% url 'core:registro' %}" class="leading-normal px-4 py-1.5 text-sm rounded-full border border-primary text-primary hover:bg-primary hover:text-white transition">Registrarse</a>{% endif %}
              <a href="#" class="leading-normal px-4 py-1.5 text-sm rounded-full bg-primary text-white hover:bg-opacity-90 transition">Iniciar Sesión</a>
            </div>
            <a href="{% url 'pedidos:carrito_ver' %}" class="relative p-2">
              <i class="fas fa-shopping-cart text-xl text-gray-700"></i>
              <span class="absolute -top-1 -right-1 bg-secondary text-white text-xs rounded-full h-5 w-5 flex items-center justify-center">0</span>
            </a>
          </div>
        </div>
      </div>
    </header>

    <!-- ======================= CONTENIDO PRINCIPAL ======================= -->
    <main class="flex-grow">
        {% block content %}
        <!-- El contenido específico de cada página se insertará aquí -->
        {% endblock content %}
    </main>

    <!-- ======================= FOOTER ======================= -->
    <!-- He copiado tu footer aquí -->
    <footer class="bg-gray-800 text-white py-10">
      <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div class="text-center md:text-left">
            <h3 class="text-xl font-bold mb-4 text-secondary">SAN BENITO</h3>
            <p class="mb-4">Ferretería y materiales de construcción de calidad</p>
            <p class="text-gray-400">© 2025 Ferretería San Benito. Todos los derechos reservados.</p>
          </div>
          <div class="text-center">
            <h3 class="text-lg font-semibold mb-4">Síguenos</h3>
            <div class="flex justify-center space-x-4">
              <a href="#" class="bg-gray-700 hover:bg-primary w-10 h-10 rounded-full flex items-center justify-center transition"><i class="fab fa-facebook-f"></i></a>
              <a href="#" class="bg-gray-700 hover:bg-primary w-10 h-10 rounded-full flex items-center justify-center transition"><i class="fab fa-instagram"></i></a>
              <a href="#" class="bg-gray-700 hover:bg-primary w-10 h-10 rounded-full flex items-center justify-center transition"><i class="fab fa-twitter"></i></a>
              <a href="#" class="bg-gray-700 hover:bg-primary w-10 h-10 rounded-full flex items-center justify-center transition"><i class="fab fa-youtube"></i></a>
            </div>
            <p class="mt-4 text-gray-400">¡Conéctate con nosotros!</p>
          </div>
          <div class="text-center md:text-right">
            <h3 class="text-lg font-semibold mb-4">Información</h3>
            <ul class="space-y-2 text-center md:text-right md:items-end flex flex-col items-center content-center">
              <li><a href="#" class="text-gray-300 hover:text-white transition flex items-center justify-end"><i class="fas fa-map-marker-alt mr-2"></i> Ubicación</a></li>
              <li><a href="{% url 'core:preguntas' %}" class="text-gray-300 hover:text-white transition flex items-center justify-end"><i class="fas fa-question-circle mr-2"></i> Preguntas frecuentes</a></li>
              <li><a href="#" class="text-gray-300 hover:text-white transition flex items-center justify-end"><i class="fas fa-truck mr-2"></i> Envíos</a></li>
              <li><a href="{% url 'core:contacto' %}" class="text-gray-300 hover:text-white transition flex items-center justify-end"><i class="fas fa-phone-alt mr-2"></i> Contacto</a></li>
            </ul>
          </div>
        </div>
      </div>
    </footer>

    <!-- Bloque para scripts JS que necesitemos más adelante -->
    {% block scripts %}{% endblock scripts %}
</body>
</html>